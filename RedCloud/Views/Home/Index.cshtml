<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bar Chart</title>
    <script src="https://cdn.jsdelivr.net/npm/chart.js@3.9.1/dist/chart.min.js"></script>
</head>
<body>
    <canvas id="canvas" width="400" height="200"></canvas>
    <script>
        // Chart.js code goes here


        const labels = ['1', '2', '3', '4', '5', '6'];
        const data = {
            labels: labels,
            datasets: [
                {
                    label: 'Message Count',
                    data: [120, 100, 140, 160, 180, 80],
                    backgroundColor: 'rgb(95, 144, 255)',
                    borderColor: 'rgba(54, 162, 235, 1)',
                    borderWidth: 1,
                    // stack: 'combined'
                },
                {
                    label: 'Day Count',
                    data: [60, 40, 20, 80, 100, 50],
                    backgroundColor: 'rgb(255, 191, 0)',
                    borderColor: 'rgba(255, 99, 132, 1)',
                    borderWidth: 1,
                    //stack: 'combined'
                },
                {
                    label: 'Inbound',
                    data: [60, 40, 20, 80, 100, 50],
                    backgroundColor: 'rgb((120,120,120)',
                    borderColor: 'rgba(255, 99, 132, 1)',
                    borderWidth: 1,
                    //stack: 'combined'
                },
                {
                    label: 'Outound',
                    data: [60, 40, 20, 80, 100, 50],
                    backgroundColor: 'rgb(195, 144, 255)',
                    borderColor: 'rgba(255, 99, 132, 1)',
                    borderWidth: 1,
                    //stack: 'combined'
                }
            ]
        };

        const config = {
            type: 'bar',
            data: data,
            options: {
                scales: {
                    yAxes: [{
                        stacked: true
                    }],
                    xAxes: [{
                        stacked: true
                    }]
                }
            }
        };

        const ctx = document.getElementById('canvas').getContext('2d');
        const myChart = new Chart(ctx, config);


        /*
         // Replace "categoryLabels" and "counts" with your actual data
         const categoryLabels = ['Category 1', 'Category 2', 'Category 3', 'Category 4'];
         const counts = [5, 10, 15, 8];

         const data = {
           labels: categoryLabels,
           datasets: [{
             label: 'Count',
             data: counts,
             backgroundColor: 'rgba(75, 192, 192, 0.2)', // Adjust color as desired
             borderColor: 'rgba(75, 192, 192, 1)',
             borderWidth: 1
           }]
         };

         const config = {
           type: 'bar',
           data: data,
           options: {
             scales: {
               yAxes: [{
                 ticks: {
                   beginAtZero: true
                 }
               }]
             }
           }
         };

         const ctx = document.getElementById('canvas').getContext('2d');
         const myChart = new Chart(ctx, config);

         */
    </script>
</body>
</html>
