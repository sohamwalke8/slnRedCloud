@*  <style>
    .rateTable {
        width: 100% !important;
    }

    .card-header {
        display: flex;
        justify-content: center;
        align-items: center;
        background-color: #007bff;
        color: white;
    }

    .card-title {
        margin: 0;
    }

    .card-body {
        padding: 20px;
    }

    .dt-search {
        display: flex;
        justify-content: flex-end;
        margin-bottom: 20px;
    }

    .table {
        margin-bottom: 0;
    }

        .table th, .table td {
            text-align: center;
            vertical-align: middle;
        }

    .modal-header {
        background-color: #dc3545;
        color: white;
    }

    .modal-footer .btn-danger {
        background-color: #dc3545;
        border: none;
    }
    .container{
        max-width: 100%;
    }
</style>

@model IEnumerable<RedCloud.Domain.Entities.GetRate>
@using Microsoft.AspNetCore.Mvc.Rendering

@{
    ViewData["Title"] = "All Rates";
}




<form method="post" id="user-query-form" action="/OrganizationAdmin/AddOrganizationAdmin" novalidate="novalidate">
    <div class="card-body">
        <div class="container mt-4">
            <div class="card shadow-lg">
                <div class="card-header">
                    <h3 class="card-title"><strong>Reseller Rate List</strong></h3>
                </div>
                <div class="card-body">
                    <div class="row mb-3">
                        <div class="col-sm-12 dt-search">
                            <a asp-action="AddRate" asp-controller="Rate" class="btn btn-primary">Add Rate</a>
                        </div>
                    </div>
                <table id="rateTable" class="table table-bordered table-striped" style="width:100% !important;">
                        <thead>
                            <tr>
                                <th hidden>Rate ID</th>
                                <th>Reseller Name</th>
                                <th>Monthly Number</th>
                                <th>Users</th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var rate in Model)
                            {
                                <tr>
                                    <td hidden>@rate.GetRateId</td>
                                    <td>@rate.GetResellerName</td>
                                    <td>@rate.GetMonthlyNumber</td>
                                    <td>@rate.GetUsers</td>
                                    <td>
                                        <a href="@Url.Action("GetRateById", "Rate", new { id = rate.GetRateId })" class="text-primary">
                                            <i class="fa-solid fa-eye"></i>
                                        </a>
                                        <a href="@Url.Action("EditRate", "Rate", new { id = rate.GetRateId })" class="text-warning ml-3">
                                            <i class="fa-regular fa-pen-to-square"></i>
                                        </a>
                                        <a href="javascript:void(0);" class="delete-btn text-danger ml-3" data-id="@rate.GetRateId" data-toggle="modal" data-target="#deleteModal">
                                            <i class="fa-solid fa-trash"></i>
                                        </a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</form>  

<div class="modal fade" id="deleteModal" tabindex="-1" role="dialog" aria-labelledby="deleteModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="deleteModalLabel">Confirm Delete</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                Are you sure you want to delete this rate?
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-dismiss="modal">Cancel</button>
                <button type="button" class="btn btn-danger" id="confirmDelete">Delete</button>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <link rel="stylesheet" type="text/css" href="https://cdn.datatables.net/1.11.5/css/jquery.dataTables.css">
    <script type="text/javascript" charset="utf8" src="https://cdn.datatables.net/1.11.5/js/jquery.dataTables.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js"></script>

    <script>
        $(document).ready(function () {
            $('#rateTable').DataTable({
                searching: true,
                paging: true
            });

            var deleteUrl = "";

            $('.delete-btn').on('click', function () {
                var rateId = $(this).data('id');
                deleteUrl = '@Url.Action("DeleteRate", "Rate")' + '?id=' + rateId;
            });

            $('#confirmDelete').on('click', function () {
                window.location.href = deleteUrl;
            });
        });
    </script>



    
} 












 *@



@model IEnumerable<RedCloud.Domain.Entities.GetRate>
@using Microsoft.AspNetCore.Mvc.Rendering

<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"]</title>

    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" />
    <!-- DataTables CSS -->
    <link rel="stylesheet" type="text/css" href="https://cdn.datatables.net/1.11.5/css/jquery.dataTables.css">
    <!-- Toastr CSS -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/toastr.js/latest/toastr.min.css" rel="stylesheet" />

    <style>
        .rateTable {
            width: 100% !important;
        }

        .card-header {
            display: flex;
            justify-content: center;
            align-items: center;
            background-color: #007bff;
            color: white;
        }

        .card-title {
            margin: 0;
        }

        .card-body {
            padding: 20px;
        }

        .dt-search {
            display: flex;
            justify-content: flex-end;
            margin-bottom: 20px;
        }

        .table {
            margin-bottom: 0;
        }

            .table th, .table td {
                text-align: center;
                vertical-align: middle;
            }

        .modal-header {
            background-color: #dc3545;
            color: white;
        }

        .modal-footer .btn-danger {
            background-color: #dc3545;
            border: none;
        }

        .container {
            max-width: 100%;
        }
        .toast {
            position: absolute;
            top: 20px;
            right: 20px;
            z-index: 1000;
        }

        .text-bg-success {
            background-color: #28a745;
            color: #fff;
        }

        .text-bg-danger {
            background-color: #dc3545;
            color: #fff;
        }
    </style>
</head>
<body>
    <form method="post" id="user-query-form" action="/OrganizationAdmin/AddOrganizationAdmin" novalidate="novalidate">
        <div class="card-body">
            <div class="container mt-4">
                <div class="card shadow-lg">
                    <div class="card-header">
                        <h3 class="card-title"><strong>Reseller Rate List</strong></h3>
                    </div>
                    <div class="card-body">
                        <div class="row mb-3">
                            <div class="col-sm-12 dt-search">
                                <a asp-action="AddRate" asp-controller="Rate" class="btn btn-primary">Add Rate</a>
                            </div>
                        </div>
                        <table id="rateTable" class="table table-bordered table-striped" style="width:100% !important;">
                            <thead>
                                <tr>
                                    <th hidden>Rate ID</th>
                                    <th>Reseller Name</th>
                                    <th>Monthly Number</th>
                                    <th>Users</th>
                                    <th>Actions</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var rate in Model)
                                {
                                    <tr>
                                        <td hidden>@rate.GetRateId</td>
                                        <td>@rate.GetResellerName</td>
                                        <td>@rate.GetMonthlyNumber</td>
                                        <td>@rate.GetUsers</td>
                                        <td>
                                            <a href="@Url.Action("GetRateById", "Rate", new { id = rate.GetRateId })" class="text-primary">
                                                <i class="fa-solid fa-eye"></i>
                                            </a>
                                            <a href="@Url.Action("EditRate", "Rate", new { id = rate.GetRateId })" class="text-warning ml-3">
                                                <i class="fa-regular fa-pen-to-square"></i>
                                            </a>
                                            <a href="javascript:void(0);" class="delete-btn text-danger ml-3" data-id="@rate.GetRateId" data-toggle="modal" data-target="#deleteModal">
                                                <i class="fa-solid fa-trash"></i>
                                            </a>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </form>

    <!-- Delete Modal -->
    <div class="modal fade" id="deleteModal" tabindex="-1" role="dialog" aria-labelledby="deleteModalLabel" aria-hidden="true">
        <div class="modal-dialog" role="document">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="deleteModalLabel">Confirm Delete</h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div class="modal-body">
                    Are you sure you want to delete this rate?
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-dismiss="modal">Cancel</button>
                    <button type="button" class="btn btn-danger" id="confirmDelete">Delete</button>
                </div>
            </div>
        </div>
    </div>

    <!-- Toastr HTML -->
    <div class="toast text-bg-success" role="alert" aria-live="assertive" aria-atomic="true">
        <div class="toast-body">
            <div class="d-flex gap-4">
                <span><i class="fa-solid fa-circle-check fa-lg"></i></span>
                <div class="d-flex flex-grow-1 align-items-center">
                    <span class="fw-semibold">Welcome to the room</span>
                    <!-- Additional content for the Toastr message -->
                </div>
            </div>
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js"></script>
    <!-- DataTables JS -->
    <script type="text/javascript" charset="utf8" src="https://cdn.datatables.net/1.11.5/js/jquery.dataTables.js"></script>
    <!-- Toastr JS -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/toastr.js/latest/toastr.min.js"></script>

    <script>
        $(document).ready(function () {


            toastr.options = {
                "closeButton": true,
                "debug": false,
                "newestOnTop": true,
                "progressBar": true,
                "positionClass": "toast-top-right",
                "preventDuplicates": true,
                "onclick": null,
                "showDuration": "300",
                "hideDuration": "1000",
                "timeOut": "5000",
                "extendedTimeOut": "1000",
                "showEasing": "swing",
                "hideEasing": "linear",
                "showMethod": "fadeIn",
                "hideMethod": "fadeOut"
            };


            $('#rateTable').DataTable({
                searching: true,
                paging: true
            });

            var deleteUrl = "";

            $('.delete-btn').on('click', function () {
                var rateId = $(this).data('id');
                deleteUrl = '@Url.Action("DeleteRate", "Rate")' + '?id=' + rateId;
            });

            $('#confirmDelete').on('click', function () {
                window.location.href = deleteUrl;
            });

            // Toastr initialization

            // Show a sample Toastr message
            toastr.success('Welcome to the room', 'Success');
        });
    </script>
</body>
</html>
